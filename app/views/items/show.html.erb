<h1> Inventory </h1>

<table>
<% user.item.each do |item| %>
	<tr>
		<td><%= user.item.name %>
		</td>
		<td><%= user.item.quantity %>
		</td>
		<td><%= if current_user.giver == false link_to "Claim", user_items_path(current_user) %>
		</td>
	</tr>
<% end %>